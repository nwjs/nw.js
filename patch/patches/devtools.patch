diff --git a/front_end/entrypoints/main/MainImpl.ts b/front_end/entrypoints/main/MainImpl.ts
index f2b153705e..67a53a26ad 100644
--- front_end/entrypoints/main/MainImpl.ts
+++ front_end/entrypoints/main/MainImpl.ts
@@ -235,6 +235,8 @@ export class MainImpl {
   }
 
   #initializeGlobalsForLayoutTests(): void {
+    // @ts-ignore layout test global
+    self.UI = self.UI || {};
     // @ts-expect-error e2e test global
     self.Extensions ||= {};
     // @ts-expect-error e2e test global
@@ -486,6 +488,8 @@ export class MainImpl {
     UI.ContextMenu.ContextMenu.installHandler(document);
     UI.ViewManager.ViewManager.instance({forceNew: true, universe: this.#universe});
 
+    // @ts-ignore layout test global
+    self.UI.inspectorView = UI.InspectorView.InspectorView.instance();
     // These instances need to be created early so they don't miss any events about requests/issues/etc.
     Logs.NetworkLog.NetworkLog.instance();
     Logs.LogManager.LogManager.instance();
diff --git a/front_end/panels/common/ExtensionServer.ts b/front_end/panels/common/ExtensionServer.ts
index 0a5ec620b1..8a34390ac5 100644
--- front_end/panels/common/ExtensionServer.ts
+++ front_end/panels/common/ExtensionServer.ts
@@ -1598,6 +1598,7 @@ export class ExtensionServer extends Common.ObjectWrapper.ObjectWrapper<EventTyp
   }
 
   static canInspectURL(url: Platform.DevToolsPath.UrlString): boolean {
+    return true;
     let parsedURL;
     // This is only to work around invalid URLs we're occasionally getting from some tests.
     // TODO(caseq): make sure tests supply valid URLs or we specifically handle invalid ones.
diff --git a/front_end/ui/helpers/OpenInNewTab.ts b/front_end/ui/helpers/OpenInNewTab.ts
index cfa93f3..7f2cda0 100644
--- front_end/ui/helpers/OpenInNewTab.ts
+++ front_end/ui/helpers/OpenInNewTab.ts
@@ -39,6 +39,42 @@ import * as Platform from '../../core/platform/platform.js';
 import * as Root from '../../core/root/root.js';
 import * as SDK from '../../core/sdk/sdk.js';
-
+
+function encodeFilePath(path: string): string {
+  const parts = path.split('/');
+  return parts.map((part, index) => {
+    if (index === 0 && /^[A-Za-z]:$/.test(part)) {
+      return part;
+    }
+    return encodeURIComponent(part);
+  }).join('/');
+}
+
+function fileURLFromAbsolutePath(path: string): URL|null {
+  if (/^[\\/]{2}/.test(path)) {
+    const normalizedPath = path.replace(/\\/g, '/');
+    return new URL(`file:${encodeFilePath(normalizedPath)}`);
+  }
+  if (path.startsWith('/')) {
+    return new URL(`file://${encodeFilePath(path)}`);
+  }
+  if (/^[A-Za-z]:[\\/]/.test(path)) {
+    const normalizedPath = path.replace(/\\/g, '/');
+    return new URL(`file:///${encodeFilePath(normalizedPath)}`);
+  }
+  return null;
+}
+
+function normalizeURLToOpen(url: URL|string): URL {
+  if (url instanceof URL) {
+    return url;
+  }
+  const fileURL = fileURLFromAbsolutePath(url);
+  if (fileURL) {
+    return fileURL;
+  }
+  return new URL(url);
+}
+
 /**
  * Opens the given `url` in a new Chrome tab.
  *
@@ -58,7 +94,7 @@ import * as SDK from '../../core/sdk/sdk.js';
  * @see https://en.wikipedia.org/wiki/UTM_parameters
  */
 export function openInNewTab(url: URL|string): void {
-  url = new URL(url);
+  url = normalizeURLToOpen(url);
   if (Common.ParsedURL.schemeIs(url, 'javascript:')) {
     return;
   }
